{
  "variables": {
    "image_repository": "amine7777/ubuntu-ansible",
    "image_tag": "latest",
    "upstream_image": "ubuntu:latest"
  },
  "builders": [
    {
      "type": "docker",
      "image": "{{ user `upstream_image` }}",
      "commit": true
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "./scripts/ubuntu.sh"
      ]
    }
  ],
  "post-processors": [
    [
      {
        "type": "docker-tag",
        "repository": "{{ user `image_repository` }}",
        "tag": "{{ user `image_tag` }}"
      },
      "docker-push"
    ]
  ]
}

